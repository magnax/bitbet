Obecny adres: <%= current_user.withdrawal_address || 'brak' %>
<%= form_for @account do |f| %>
	<%= render 'shared/error_messages', :object => @account %>
	<div class="field">
		<%= f.label :nr, "Numer konta BTC:", class: "control-label" %>
		<%= f.text_field :nr %>
	</div>
    <div class="actions">
		<%= f.submit "Zapisz" %>
	</div>
<% end %>